@using HotelApp.Core;
@model Cuarto;
@{
    var texto = Model is null ? "Alta Cuarto" : "Modificar Cuarto";
    ViewData["Title"] = texto;
}

<form asp-controller="Cuarto" asp-action="Upsert" method="post">
    <div asp-validation-summary="All"></div>
    @if (Model != null)
    {
        <div><input type="hidden" asp-for="@Model.IdCuarto" /></div>
    }
    <div class="mb-3">
        <label asp-for="Cochera" class="form-label">Cochera</label>
        <input asp-for="Cochera" class="form-check-input" type="checkbox" />
    </div>
    <div class="mb-3">
        <label asp-for="Noche" class="form-label">Noche</label>
        <input asp-for="Noche" class="form-control" type="number"/>
    </div>
    <div class="mb-3">
        <label asp-for="Descripcion" class="form-label">Descripción</label>
        <input asp-for="Descripcion" class="form-control" />
    </div>
    <div class="mb-3">
        @if (Model is null)
        {
            <button type="submit" class="btn btn-primary">@texto</button>
        }
        else
        {
            <button type="submit" class="btn btn-warning">@texto</button>
        }
    </div>
</form>