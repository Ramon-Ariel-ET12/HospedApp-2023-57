@using HotelApp.Core;
@model IEnumerable<Cliente>;
@{
    ViewData["Title"] = "Clientes";
}

<div class="container mt-5">
    <div class="text-center">
        <h1 class="display-4">Buscar Clientes</h1>
    </div>

    <div class="text-center mt-4">
        <form asp-controller="Cliente" asp-action="Buscar" method="get" class="form-inline justify-content-center">
            <input type="text" name="busqueda" class="form-control mr-2" placeholder="Buscar clientes..." />
            <button type="submit" class="btn btn-primary">Buscar</button>
        </form>
    </div>

    <div class="content-box mt-4">
        @if (Model.Count() == 0 && Context.Request.RouteValues.ContainsKey("Busqueda"))
        {
            <p class="text-muted">No se encontraron clientes.</p>
        }
        else
        {
            <h3 class="mt-4">Lista de Clientes</h3>
            <div class="table-responsive">
                <table class="table table-striped table-hover mt-3">
                    <thead class="thead-dark">
                        <tr>
                            <th scope="col">Dni</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Apellido</th>
                            <th scope="col">Email</th>
                            <th scope="col">Acción</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var cliente in Model)
                        {
                            <tr>
                                <th>@cliente.Dni</th>
                                <td>@cliente.Nombre</td>
                                <td>@cliente.Apellido</td>
                                <td>@cliente.Email</td>
                                <td>
                                    <a asp-action="Modificar" asp-route-dni="@cliente.Dni" class="btn btn-sm btn-outline-secondary">
                                        Modificar <i class="bi bi-pencil-fill"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>